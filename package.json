{
  "name": "htd-zone-card",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "test": "vitest run",
    "watch": "node build.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "lit": "^3.2.0",
    "vitest": "^2.1.0"
  }
}
